export type User = {
    client: string;
    email: string;
    firstName: string;
    fullName: string;
    id: string;
    language: string;
    lastName: string;
    system: string;
    timeZone: string;
}
